import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  title: {
    color: '#FFF',
    fontWeight: 600,
    fontSize: 28,
    textAlign: 'center',
    marginTop: 50,
  },
  subtitle: {
    color: '#FFF',
    fontWeight: 600,
    textAlign: 'center',
    marginTop: 25,
    fontSize: 24,
    marginBottom: 40,
  },
}));

export default useStyles;
